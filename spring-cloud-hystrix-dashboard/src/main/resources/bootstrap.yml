server:
  port: 9099
spring:
  #cloud:
    #config:
      # 读取配置的GitLab分支
      #label: master
      # 读取Spring-Cloud-Config配置中心的配置
      #uri: http://localhost:9090
      #name: config-info
      #profile: dev
  application:
    name: spring-cloud-hystrix-turbine-dashboard